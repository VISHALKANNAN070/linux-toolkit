# Clean Light Mode Config
use_shadow = ON
use_colors = ON

# Screen background: White with Black text
screen_color = (BLACK,WHITE,OFF)

# Dialog box background: White
dialog_color = (BLACK,WHITE,OFF)
title_color = (BLUE,WHITE,ON)
border_color = (BLUE,WHITE,ON)

# Buttons
button_active_color = (WHITE,BLUE,ON)
button_inactive_color = (BLACK,WHITE,OFF)
button_key_active_color = (WHITE,BLUE,ON)
button_key_inactive_color = (RED,WHITE,OFF)
button_label_active_color = (YELLOW,BLUE,ON)
button_label_inactive_color = (BLACK,WHITE,ON)

# Input fields
inputbox_color = (BLACK,WHITE,OFF)
inputbox_border_color = (BLUE,WHITE,ON)
searchbox_color = (BLACK,WHITE,OFF)
searchbox_title_color = (BLUE,WHITE,ON)
searchbox_border_color = (RED,WHITE,ON)
position_indicator_color = (RED,WHITE,ON)

# Menu Items
menubox_color = (BLACK,WHITE,OFF)
menubox_border_color = (BLUE,WHITE,ON)
item_color = (BLACK,WHITE,OFF)
item_selected_color = (WHITE,BLUE,ON)

# Tags (Checkboxes/Radio buttons)
tag_color = (BLUE,WHITE,ON)
tag_selected_color = (YELLOW,BLUE,ON)
tag_key_color = (RED,WHITE,ON)
tag_key_selected_color = (YELLOW,BLUE,ON)
check_color = (BLACK,WHITE,OFF)
check_selected_color = (WHITE,BLUE,ON)

# Scroll arrows
uarrow_color = (GREEN,WHITE,ON)
darrow_color = (GREEN,WHITE,ON)

# Help text
itemhelp_color = (WHITE,BLACK,OFF)

# Forms and Gauges
form_active_text_color = (WHITE,BLUE,ON)
form_text_color = (BLUE,WHITE,ON)
form_item_readonly_color = (CYAN,WHITE,ON)
gauge_color = (BLUE,WHITE,ON)

# Secondary borders
border2_color = (BLUE,WHITE,ON)
inputbox_border2_color = (BLUE,WHITE,ON)
searchbox_border2_color = (BLUE,WHITE,ON)
menubox_border2_color = (BLUE,WHITE,ON)
